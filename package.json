{
  "type": "module",
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.3",
    "gzip-cli": "^1.2.0",
    "rollup": "^3.23.0",
    "vitepress": "^1.0.0-beta.4"
  },
  "scripts": {
    "minify": "rollup --config",
    "build": "rollup --config && gzip dist/flect.js",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "prettier": {
    "tabWidth": 4
  }
}